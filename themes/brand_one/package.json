{
  "name": "brand_one",
  "version": "1.0.0",
  "description": "A Drupal theme whose Twig templates are integrated with Pattern Lab.",
  "main": "gulpfile.js",
  "scripts": {
    "start": "bower install && gulp clean && gulp compile && gulp",
    "compile": "gulp clean && gulp compile",
    "test": "gulp validate"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "bourbon": "^4.2.7",
    "breakpoint-sass": "^2.7.0",
    "gulp": "^3.9.1",
    "gulp-help": "^1.6.1",
    "js-yaml": "^3.6.0",
    "lodash.merge": "^4.3.5",
    "normalize.scss": "^0.1.0",
    "p2-theme-core": "^7.0.1",
    "semver": "^5.1.0",
    "singularitygs": "^1.7.0"
  }
}
